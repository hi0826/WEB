extends layouts/main

block content
    .form-container
        form(action=`/user${routes.changePassword}`, method="post")
            input(type="password", name="oldPassword", placegolder="Current Password")
            input(type="password", name="newPassword", placegolder="New Password")
            input(type="password", name="newPassword1", placegolder="Verify New Password")
            input(type="submit", value="Change Password")